import React from "react";
import BaseLayout from "../components/layout/BaseLayout";

const Contacts = props => {
  return <BaseLayout {...props.auth}>Contacts page</BaseLayout>;
};

export default Contacts;
